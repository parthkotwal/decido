services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/decido.db
    volumes:
      - decido-data:/app/data
    restart: unless-stopped

volumes:
  decido-data:
